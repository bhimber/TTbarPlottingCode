
cd TTbarDilepPlotting
set up Rootcore by:
source setup.sh
Then cd DPlot.git
source setup.sh, it sets up Python and add it to PATH.
cd ..
cd plotting
python ttbardilep.py ttbardilep.json
Remember to remove any old root files before running; otherwise the script will complain that they already exist and will quit.…the
the config file,ttbardilep.json has information about the list of 
the data files that are inside datafiles.txt, all the files are read and then python 
produces a root file with the name ttbarelmu.root that has been alrady a part of config file. The flag,weightand channel 
have been setup inside .json files.
